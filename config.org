#+TITLE: Emacs Configuration
#+AUTHOR: Anderson Saunders
#+OPTIONS: toc:nil num:nil

* Package Management

#+begin_src emacs-lisp
;; Straight.el is configured in init.el
;; All packages installed via straight.el (git-based)

;; Use-package verbose mode for debugging
(setq use-package-verbose t)

;; Auto-compile packages
(use-package auto-compile
  :config (auto-compile-on-load-mode))

;; Always load newest version
(setq load-prefer-newer t)

;; Reduce byte-compile warnings
(setq byte-compile-warnings '(cl-functions))
#+end_src

* Sensible Defaults

#+begin_src emacs-lisp
;; Utility functions
(defun ando/comment-or-uncomment-region-or-line ()
  "Comments or uncomments the region or the current line if
there's no active region."
  (interactive)
  (let (beg end)
    (if (region-active-p)
        (setq beg (region-beginning) end (region-end))
      (setq beg (line-beginning-position) end (line-end-position)))
    (comment-or-uncomment-region beg end)))

(defun ando/reset-text-size ()
  (interactive)
  (text-scale-set 0))

;; Settings
(setq vc-follow-symlinks t)                ; Don't prompt when visiting symlinked files
(add-hook 'after-save-hook 'executable-make-buffer-file-executable-if-script-p)
(setq sentence-end-double-space nil)       ; Affects M-a/M-e sentence navigation
(add-hook 'before-save-hook
          (lambda ()
            (when buffer-file-name
              (let ((dir (file-name-directory buffer-file-name)))
                (when (and (not (file-exists-p dir))
                           (y-or-n-p (format "Directory %s does not exist. Create it?" dir)))
                  (make-directory dir t))))))
(setq require-final-newline t)
(setq confirm-kill-emacs 'y-or-n-p)
(setq inhibit-startup-message t)
(setq initial-scratch-message nil)
(setq use-short-answers t)                 ; y/n instead of yes/no
(show-paren-mode t)
(setq show-paren-delay 0.0)
(setq show-paren-style 'expression)        ; Highlight entire expression between parens
(setq visible-bell t)                      ; Flash screen instead of audible beep
(setq-default fill-column 80)
(setq ns-pop-up-frames nil)                ; macOS: open files in existing frame, not a new one
(setq mouse-yank-at-point t)               ; Middle-click paste at point, not at mouse position

;; Backups to temp directory
(setq backup-directory-alist
      `((".*" . ,temporary-file-directory)))
(setq auto-save-file-name-transforms
      `((".*" ,temporary-file-directory t)))
;; Store .#lockfiles in /var/tmp instead of alongside the file
(setq lock-file-name-transforms
      '(("\\`/.*/\\([^/]+\\)\\'" "/var/tmp/\\1" t)))

;; Keybindings
(global-set-key (kbd "M-;") 'ando/comment-or-uncomment-region-or-line)
(global-set-key (kbd "<home>") 'move-beginning-of-line)
(global-set-key (kbd "<end>") 'move-end-of-line)
(define-key global-map (kbd "C-)") 'ando/reset-text-size)
(define-key global-map (kbd "C-+") 'text-scale-increase)
(define-key global-map (kbd "C-=") 'text-scale-increase)
(define-key global-map (kbd "C-_") 'text-scale-decrease)
(define-key global-map (kbd "C--") 'text-scale-decrease)
#+end_src

* Personal Information

#+begin_src emacs-lisp
(setq user-full-name "Ando"
      calendar-latitude -28.10
      calendar-longitude 153.42
      calendar-location-name "Gold Coast, QLD, Australia"
      calendar-date-style "european")
#+end_src

* Large files and Garbage Collection

#+begin_src emacs-lisp
(setq large-file-warning-threshold 100000000) ; ~100MB before Emacs warns about file size

(use-package gcmh
  :config (gcmh-mode 1))
#+end_src

* Cursor & Regex

#+begin_src emacs-lisp
(blink-cursor-mode t)
(setq reb-re-syntax 'string) ; re-builder uses string syntax (no double-escaping backslashes)
#+end_src

* Org-mode and Roam

#+begin_src emacs-lisp
(setq org-adapt-indentation nil)
(setq org-startup-indented t)
(setq org-edit-src-content-indentation 0)

;; Use overlays instead of text-properties for folding. Without this,
;; consult-line won't show lines for folded/hidden org headings.
;; TODO: Remove once org-mode and consult-line improve compatibility with
;; the default 'text-properties fold style.
(setq org-fold-core-style 'overlays)

(use-package org-modern
  :hook (org-mode . org-modern-mode)
  :custom
  (org-modern-star '("◉" "○" "◈" "◇" "▸"))
  (org-modern-hide-stars nil))

(use-package org-modern-indent
  :straight (:host github :repo "jdtsmith/org-modern-indent")
  :hook (org-indent-mode . org-modern-indent-mode))

(use-package org-roam
  :custom
  (org-roam-directory "~/Documents/notes/org-roam")
  (org-roam-dailies-directory "daily/")
  (org-roam-completion-everywhere t)
  (org-roam-capture-templates
   '(("p" "Personal" plain "%?"
      :target (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                          "#+title: ${title}\n#+filetags: :personal:\n")
      
      :unnarrowed t)
     ("w" "Work" plain "%?"
      :target (file+head "%<%Y%m%d%H%M%S>-${slug}.org"
                          "#+title: ${title}\n#+filetags: :work:\n")
      :unnarrowed t)))
  :bind (("C-c n l" . org-roam-buffer-toggle)
         ("C-c n f" . org-roam-node-find)
         ("C-c n i" . org-roam-node-insert)
         ("C-c n c" . org-roam-capture)
         ("C-c n j" . org-roam-dailies-capture-today)
         ("C-c n d" . org-roam-dailies-goto-today))
  :config
  ;; Include tags in the node display so they are searchable in org-roam-node-find.
  ;; 50 reserves enough columns for tags to be visible alongside the flexible-width title.
  (setq org-roam-node-display-template
        (concat "${title:*} " (propertize "${tags:50}" 'face 'org-tag)))
  (org-roam-db-autosync-mode))

(use-package org-roam-ui
  :after org-roam
  :custom
  (org-roam-ui-sync-theme t)
  (org-roam-ui-follow t)
  (org-roam-ui-update-on-save t)
  (org-roam-ui-open-on-start t))

(global-set-key (kbd "C-c c") #'org-capture)
(setq org-capture-templates
      '(("p" "Personal Quick Note" entry
         (file+headline "~/Documents/notes/org-roam/inbox.org" "Personal")
         "\n* %? - %U" :prepend t :empty-lines-after 1)
        ("w" "Work Quick Note" entry
         (file+headline "~/Documents/notes/org-roam/inbox.org" "Work")
         "\n* %? - %U" :prepend t :empty-lines-after 1)))
(add-hook 'auto-save-hook 'org-save-all-org-buffers)
#+end_src

* UI Preferences

** Window Chrome

#+begin_src emacs-lisp
(tool-bar-mode -1)
(menu-bar-mode 0)
(scroll-bar-mode -1)
(set-window-scroll-bars (minibuffer-window) nil nil)
#+end_src

** Scrolling

#+begin_src emacs-lisp
(setq scroll-margin 0
      scroll-conservatively 100           ; Scroll line-by-line instead of jumping half-screen
      scroll-preserve-screen-position 1)  ; Keep cursor at same screen position when scrolling
#+end_src

** Mode Line

#+begin_src emacs-lisp
(line-number-mode t)
(column-number-mode t)
(size-indication-mode t)
#+end_src

** Frame Title

#+begin_src emacs-lisp
(setq frame-title-format
      '((:eval (if (buffer-file-name)
                   (abbreviate-file-name (buffer-file-name))
                 "%b"))))
#+end_src

** Indentation & Tabs

#+begin_src emacs-lisp
(setq-default indent-tabs-mode nil
              tab-width 2)
(setq js-indent-level 2
      typescript-ts-mode-indent-offset 2
      css-indent-offset 2)
#+end_src

** Indentation Guides

#+begin_src emacs-lisp
(use-package indent-bars
  :straight (:host github :repo "jdtsmith/indent-bars")
  :hook (prog-mode . indent-bars-mode)
  :custom
  (indent-bars-prefer-character t)
  (indent-bars-treesit-support t)
  (indent-bars-no-descend-string t)
  (indent-bars-treesit-ignore-blank-lines-types '("module"))
  (indent-bars-treesit-wrap '((ruby argument_list method_parameters)
                              (typescript arguments formal_parameters
                                          object object_pattern
                                          array array_pattern)
                              (tsx arguments formal_parameters
                                   object object_pattern
                                   array array_pattern
                                   jsx_expression)))
  (indent-bars-width-frac 0.15)
  (indent-bars-highlight-current-depth '(:pattern "."))
  (indent-bars-treesit-scope '((ruby method class module if unless case
                                      while until for do_block begin)
                               (typescript function_declaration
                                           arrow_function method_definition
                                           class_declaration if_statement
                                           for_statement while_statement
                                           switch_statement try_statement)
                               (tsx function_declaration
                                    arrow_function method_definition
                                    class_declaration if_statement
                                    for_statement while_statement
                                    switch_statement try_statement))))
#+end_src

** Tree-sitter (Built-in)

#+begin_src emacs-lisp
;; Built-in tree-sitter configuration (Emacs 29+)
(setq treesit-language-source-alist
      '((bash "https://github.com/tree-sitter/tree-sitter-bash")
        (css "https://github.com/tree-sitter/tree-sitter-css")
        (elisp "https://github.com/Wilfred/tree-sitter-elisp")
        (html "https://github.com/tree-sitter/tree-sitter-html")
        (javascript "https://github.com/tree-sitter/tree-sitter-javascript" "master" "src")
        (json "https://github.com/tree-sitter/tree-sitter-json")
        ;; TODO: Revert to release branch once a new kotlin tree-sitter release is out
        (kotlin "https://github.com/fwcd/tree-sitter-kotlin" "main" "src")
        (ruby "https://github.com/tree-sitter/tree-sitter-ruby")
        (tsx "https://github.com/tree-sitter/tree-sitter-typescript" "master" "tsx/src")
        (typescript "https://github.com/tree-sitter/tree-sitter-typescript" "master" "typescript/src")
        (yaml "https://github.com/ikatyang/tree-sitter-yaml")))

;; Auto-install grammars if missing
(defun my-install-treesit-grammars ()
  "Install tree-sitter grammars if they don't exist."
  (interactive)
  (dolist (lang treesit-language-source-alist)
    (unless (treesit-language-available-p (car lang))
      (message "Installing tree-sitter grammar for %s..." (car lang))
      (treesit-install-language-grammar (car lang)))))

;; Install grammars on first load if needed
(when (not (treesit-language-available-p 'typescript))
  (my-install-treesit-grammars))
#+end_src

** Whitespace

#+begin_src emacs-lisp
(add-hook 'prog-mode-hook (lambda () (setq show-trailing-whitespace t)))
(add-hook 'before-save-hook
          (lambda ()
            (when (derived-mode-p 'prog-mode)
              (delete-trailing-whitespace))))
#+end_src

** Org Source Indentation

#+begin_src emacs-lisp
(setq org-src-preserve-indentation t) ; Don't add extra indentation inside org src blocks on export/tangle
#+end_src

** Selection

#+begin_src emacs-lisp
(delete-selection-mode t)
#+end_src

** Buffers

#+begin_src emacs-lisp
(global-auto-revert-mode t)
#+end_src

** Winner Mode

#+begin_src emacs-lisp
(winner-mode 1)
#+end_src

** Narrowing

#+begin_src emacs-lisp
(put 'narrow-to-defun  'disabled nil)
(put 'narrow-to-page   'disabled nil)
(put 'narrow-to-region 'disabled nil)
#+end_src

** Subword

#+begin_src emacs-lisp
(use-package subword
  :straight nil
  :config (global-subword-mode 1))
#+end_src

** UTF-8

#+begin_src emacs-lisp
(prefer-coding-system 'utf-8)
(set-default-coding-systems 'utf-8)
(set-terminal-coding-system 'utf-8)
(set-keyboard-coding-system 'utf-8)
#+end_src

** Tab Key

#+begin_src emacs-lisp
(setq tab-always-indent 'complete) ; TAB triggers completion when line is already indented
#+end_src

** Highlight Line

#+begin_src emacs-lisp
(use-package hl-line
  :straight nil
  :config
  (global-hl-line-mode +1))
#+end_src

** Theme

#+begin_src emacs-lisp
(load-theme 'modus-operandi t)
#+end_src

** Uniquify

#+begin_src emacs-lisp
(setq uniquify-buffer-name-style 'forward) ; Show path prefix for same-name buffers (dir1/file vs dir2/file)
#+end_src

** Volatile Highlights

#+begin_src emacs-lisp
(use-package volatile-highlights
  :config
  (volatile-highlights-mode +1))
#+end_src

** Savefiles Directory

#+begin_src emacs-lisp
(defconst ando-savefiles-dir (expand-file-name "savefiles" user-emacs-directory))
(unless (file-exists-p ando-savefiles-dir)
  (make-directory ando-savefiles-dir))
#+end_src

** Recent Files

#+begin_src emacs-lisp
(use-package recentf
  :straight nil
  :config
  (setq recentf-save-file (expand-file-name "recentf" ando-savefiles-dir)
        recentf-max-saved-items 500
        recentf-max-menu-items 15
        recentf-auto-cleanup 'never) ; Don't prune dead files from list on startup (slow over TRAMP)
  (recentf-mode +1))
#+end_src

** Savehist

#+begin_src emacs-lisp
(use-package savehist
  :straight nil
  :config
  (setq savehist-additional-variables
        '(search-ring regexp-search-ring)
        savehist-autosave-interval 60
        savehist-file (expand-file-name "savehist" ando-savefiles-dir))
  (savehist-mode +1))
#+end_src

** Line Numbers

#+begin_src emacs-lisp
(global-display-line-numbers-mode)
#+end_src

** Code Folding with treesit-fold

#+begin_src emacs-lisp
(use-package treesit-fold
  :straight (:host github :repo "emacs-tree-sitter/treesit-fold")
  :config
  ;; Enable folding in tree-sitter modes
  (add-hook 'ruby-ts-mode-hook #'treesit-fold-mode)
  (add-hook 'typescript-ts-mode-hook #'treesit-fold-mode)
  (add-hook 'tsx-ts-mode-hook #'treesit-fold-mode)
  (add-hook 'js-ts-mode-hook #'treesit-fold-mode)
  (add-hook 'kotlin-ts-mode-hook #'treesit-fold-mode)
  ;; Optional: Add keybindings
  (global-set-key (kbd "C-c f t") 'treesit-fold-toggle)
  (global-set-key (kbd "C-c f c") 'treesit-fold-close)
  (global-set-key (kbd "C-c f o") 'treesit-fold-open)
  (global-set-key (kbd "C-c f C") 'treesit-fold-close-all)
  (global-set-key (kbd "C-c f O") 'treesit-fold-open-all))
#+end_src

** Fold-this

#+begin_src emacs-lisp
(use-package fold-this)
#+end_src

** Magit and Forge

#+begin_src emacs-lisp
;; Magit dependency
(use-package cond-let
  :straight (:host github :repo "tarsius/cond-let"))

(use-package magit
  :bind (("C-x g" . magit-status))
  :config
  (setq magit-save-repository-buffers nil)       ; Don't auto-save buffers on magit operations
  (setq magit-list-refs-sortby "-committerdate")) ; Most recently committed branches first
(use-package forge
  :after magit
  :config
  (setq forge-pull-requests-limit 50))
#+end_src

** Electric-pair

#+begin_src emacs-lisp
(electric-pair-mode 1)
#+end_src


* My Functions

#+begin_src emacs-lisp
(defun ando-run-macro-until-line (line-number)
  "Run the last keyboard macro until LINE-NUMBER is reached."
  (interactive "nRun macro until line: ")
  (while (< (line-number-at-pos) line-number)
    (call-last-kbd-macro)))

(defun ando-yaml-indentation-level (s)
  (if (string-match "^ " s)
      (+ 1 (ando-yaml-indentation-level (substring s 1)))
    0))

(defun ando-yaml-current-line ()
  (buffer-substring-no-properties (line-beginning-position) (line-end-position)))

(defun ando-yaml-clean-string (s)
  (let* ((s (replace-regexp-in-string "^[[:blank:]-:]*" "" s))
         (s (replace-regexp-in-string ":$" "" s)))
    s))

(defun ando-yaml-not-blank-p (s)
  (string-match "[^[:blank:]]" s))

(defun ando-yaml-path-to-point ()
  (save-excursion
    (let* ((line (ando-yaml-current-line))
           (level (ando-yaml-indentation-level line))
           result)
      (while (> (point) (point-min))
        (beginning-of-line 0)
        (setq line (ando-yaml-current-line))
        (let ((new-level (ando-yaml-indentation-level line)))
          (when (and (ando-yaml-not-blank-p line)
                     (< new-level level))
            (setq level new-level)
            (setq result (push (ando-yaml-clean-string line) result)))))
      (mapconcat 'identity result " => "))))

(defun ando-yaml-show-path-to-point ()
  (interactive)
  (message (ando-yaml-path-to-point)))

(with-eval-after-load 'yaml-ts-mode
  (define-key yaml-ts-mode-map (kbd "C-c y p") #'ando-yaml-show-path-to-point))

(defun ando-wait-for-buffer (buffer-name)
  "Wait for the specified buffer to open."
  (while (not (get-buffer buffer-name))
    (sit-for 0.1)))

(defun ando-ruby-namespaced ()
  (interactive)
  (let ((buffer (get-buffer "*eldoc*")))
    (when buffer
      (kill-buffer buffer)
      (message "Buffer '%s' killed." "*eldoc*")))
  (eldoc--invoke-strategy t)
  (ando-wait-for-buffer "*eldoc*")
  (with-current-buffer "*eldoc*"
    (goto-char (point-min))
    (kill-new (buffer-substring (line-beginning-position) (line-end-position)))
    (kill-buffer "*eldoc*")))

(defun ando-copy-filename-to-clipboard ()
  "Copy the current buffer file name to the clipboard."
  (interactive)
  (let ((filename (if (equal major-mode 'dired-mode)
                      default-directory
                    (buffer-file-name))))
    (when filename
      (kill-new filename)
      (message "Copied buffer file name '%s' to the clipboard." filename))))

(defun ando-get-repo-name ()
  "Get magit current repo's name"
  (nth 0
       (last
        (split-string
         (magit-toplevel) "/") 2)))

(defun ando-file-path ()
  "Get file-path relative to repo"
  (string-join (nthcdr 5 (split-string buffer-file-name "/")) "/"))

(defun ando-file-in-ca-gh ()
  "Copy a file to its address in master on CA Github"
  (interactive)
  (let ((file-gh-address (format "https://github.com/cultureamp/%s/blob/master/%s#L%s"
                                 (ando-get-repo-name)
                                 (ando-file-path)
                                 (line-number-at-pos))))
    (kill-new file-gh-address)
    (message "Copied file's github address '%s' to the clipboard." file-gh-address)))

(defun ando-ticket-url ()
  "Open the jira ticket related to the current branch"
  (interactive)
  (let* ((jira-prefix "https://cultureamp.atlassian.net/browse/")
         (branch-ticket (nth 0 (split-string (magit-get-current-branch) "/"))))
    (format "%s%s" jira-prefix branch-ticket)))

(defun ando-insert-ticket-url ()
  "insert jira ticket related to the current branch"
  (interactive)
  (insert (ando-ticket-url)))

(defun ando-open-ticket ()
  "Open the jira ticket related to the current branch"
  (interactive)
  (let ((ticket-url (ando-ticket-url)))
    (open_in_browser ticket-url)
    (message "Opened ticket: %s" ticket-url)))

(defalias 'open_in_browser 'browse-url-default-macosx-browser)

(defun ando-insert-formatted-date ()
  "Prompt for a date format and insert the resulting formatted date at point."
  (interactive)
  (let* ((formats '(("%Y%m%d" . "YYYYMMDD")
                    ("%Y%m%d%H%M%S" . "YYYYMMDDHHMMSS")
                    ("%Y-%m-%d" . "YYYY-MM-DD")
                    ("%Y-%m-%d %H:%M:%S" . "YYYY-MM-DD HH:MM:SS")
                    ("%d-%m-%Y" . "DD-MM-YYYY")
                    ("%d/%m/%Y" . "DD/MM/YYYY")
                    ("%H:%M:%S" . "HH:MM:SS")))
         (choices (mapcar #'cdr formats))
         (selection (completing-read "Choose date format: " choices))
         (format-string (car (rassoc selection formats)))
         (date (shell-command-to-string (concat "date +\"" format-string "\""))))
    (insert (string-trim date))))
#+end_src

* Project Management

#+begin_src emacs-lisp
;; Built-in project.el (Emacs 29+)
(use-package project
  :straight nil
  :bind (("C-c p f" . project-find-file)
         ("C-c p g" . project-find-regexp)
         ("C-c p d" . project-find-dir)
         ("C-c p b" . consult-project-buffer)
         ("C-c p p" . project-switch-project)
         ("C-c p c" . project-compile)
         ("C-c p k" . project-kill-buffers)
         ("C-c p !" . project-shell-command)
         ("C-x f"   . project-find-file))
  :config
  (setq project-switch-commands
        '((project-find-file "Find file" ?f)
          (consult-ripgrep "Ripgrep" ?g)
          (project-find-dir "Find dir" ?d)
          (consult-project-buffer "Buffer" ?b)
          (project-shell "Shell" ?s))))
#+end_src

* Completion Framework (Vertico Ecosystem)

#+begin_src emacs-lisp
;; Vertico - vertical completion UI
(use-package vertico
  :init
  (vertico-mode)
  :config
  (setq vertico-cycle t)                 ; Wrap around at top/bottom of candidate list
  (setq enable-recursive-minibuffers t)  ; Allow commands inside the minibuffer (e.g., M-x while in M-x)
  (define-key vertico-map (kbd "M-SPC") #'embark-select)
  ;; Prepend file-type nerd-icon to group titles (e.g., filenames in consult-ripgrep).
  ;; Uses :around advice to intercept the title before vertico formats it.
  (with-eval-after-load 'nerd-icons
    (define-advice vertico--format-group-title (:around (orig title cand) nerd-icons)
      (funcall orig (concat (nerd-icons-icon-for-file title) " " title) cand))))


;; Orderless - flexible matching
(use-package orderless
  :custom
  (completion-styles '(orderless basic))
  (completion-category-defaults nil)
  (completion-category-overrides '((file (styles partial-completion)))))

;; Marginalia - rich annotations
(use-package marginalia
  :init
  (marginalia-mode)
  :bind (:map minibuffer-local-map
              ("M-A" . marginalia-cycle)))

;; Consult - enhanced commands
(use-package consult
  :bind (("C-y" . consult-yank-from-kill-ring)
         ("M-y" . consult-yank-pop)
         ("C-s" . consult-line)
         ("C-x b" . consult-buffer)
         ("C-c C-r" . consult-recent-file)
         ("C-c j" . consult-git-grep)
         ("C-c k" . consult-ripgrep)
         ("C-x l" . consult-locate)
         ("C-c g" . consult-grep)
         :map minibuffer-local-map
         ("C-r" . consult-history))
  :config
  (setq consult-narrow-key "<")                  ; Press < to filter by category in consult
  (setq consult-point-placement 'match-beginning)) ; Jump to start of match, not end

;; Embark - contextual actions
(use-package embark
  :bind (("C-." . embark-act)
         ("C-h B" . embark-bindings))
  :config
  (setq prefix-help-command #'embark-prefix-help-command) ; C-h after prefix key shows embark menu
  (define-key embark-general-map (kbd "G") #'consult-ripgrep))

;; Embark-Consult integration
(use-package embark-consult
  :after (embark consult)
  :hook
  (embark-collect-mode . consult-preview-at-point-mode))
#+end_src

* Programming Languages

** TypeScript, JavaScript, and Web Development

#+begin_src emacs-lisp
;; TypeScript and JavaScript with built-in tree-sitter modes
(add-to-list 'auto-mode-alist '("\\.tsx\\'" . tsx-ts-mode))
(add-to-list 'auto-mode-alist '("\\.ts\\'" . typescript-ts-mode))
(add-to-list 'auto-mode-alist '("\\.jsx\\'" . tsx-ts-mode))
(add-to-list 'auto-mode-alist '("\\.js\\'" . js-ts-mode))


;; TypeScript/JavaScript indentation is set in "Indentation & Tabs" section
#+end_src

** Kotlin

#+begin_src emacs-lisp
;; Kotlin with tree-sitter
(use-package kotlin-ts-mode
  :straight (:host github :repo "emacsmirror/kotlin-ts-mode")
  :mode "\\.kt\\'"
  :config
  (setq kotlin-ts-mode-indent-offset 4))
#+end_src

** Ruby

#+begin_src emacs-lisp
;; Ruby with built-in tree-sitter mode
(add-to-list 'auto-mode-alist '("\\.rb\\'" . ruby-ts-mode))
(add-to-list 'auto-mode-alist '("\\.rbs\\'" . ruby-ts-mode))
(add-to-list 'auto-mode-alist '("Gemfile\\'" . ruby-ts-mode))
(add-to-list 'auto-mode-alist '("Rakefile\\'" . ruby-ts-mode))

;; Ruby indentation settings
(setq ruby-indent-level 2)
(setq ruby-indent-tabs-mode nil)
(setq ruby-deep-indent-paren-style 'space) ; Don't deep-indent args to match opening paren
(setq ruby-method-call-indent nil)         ; Don't extra-indent chained method calls
(setq ruby-method-params-indent 0)         ; Don't indent method params beyond the def
#+end_src

* Code Editing Tools

#+begin_src emacs-lisp
(use-package yasnippet
  :bind (:map yas-minor-mode-map
              ("TAB" . nil)
              ("<tab>" . nil))
  :config (yas-global-mode))

(use-package yasnippet-snippets
  :after yasnippet)

(use-package expand-region
  :bind (("C-c e" . er/expand-region)))
#+end_src

* Completion UI (Corfu & Cape)

#+begin_src emacs-lisp
;; Corfu - modern completion UI
(use-package corfu
  :custom
  (corfu-cycle t)                    ; Enable cycling for completion
  (corfu-auto t)                     ; Enable auto completion
  (corfu-auto-delay 0.1)             ; Delay before showing completions
  (corfu-auto-prefix 2)              ; Minimum prefix length for auto completion
  (corfu-popupinfo-delay 0.5)        ; Show documentation after delay
  :bind
  (:map corfu-map
        ("TAB" . corfu-next)
        ([tab] . corfu-next)
        ("S-TAB" . corfu-previous)
        ([backtab] . corfu-previous))
  :init
  (global-corfu-mode)
  (corfu-popupinfo-mode))

;; Kind-icon - show icons in completion popup
(use-package kind-icon
  :after corfu
  :custom
  (kind-icon-default-face 'corfu-default)
  :config
  (add-to-list 'corfu-margin-formatters #'kind-icon-margin-formatter))

;; Cape - Completion At Point Extensions
(use-package cape
  :init
  ;; Add completion backends
  (add-to-list 'completion-at-point-functions #'cape-dabbrev)
  (add-to-list 'completion-at-point-functions #'cape-file)
  (add-to-list 'completion-at-point-functions #'cape-keyword))
#+end_src

* Language Server Protocol (LSP)

#+begin_src emacs-lisp
(use-package eglot
  :defer t
  :commands (eglot eglot-ensure)
  :hook (
         (ruby-ts-mode . eglot-ensure)
         (typescript-ts-mode . eglot-ensure)
         (tsx-ts-mode . eglot-ensure)
         (js-ts-mode . eglot-ensure)
         ;; (kotlin-ts-mode . eglot-ensure)
         (rust-mode . eglot-ensure))
  :config
  (add-to-list 'eglot-server-programs '(ruby-ts-mode "ruby-lsp"))
  (add-to-list 'eglot-server-programs '((typescript-ts-mode tsx-ts-mode js-ts-mode) "typescript-language-server" "--stdio"))
  ;; Kotlin LSP (official JetBrains)
  (add-to-list 'eglot-server-programs '(kotlin-ts-mode "~/.emacs.d/lsp-servers/kotlin/kotlin-lsp.sh" "--stdio"))
  )
#+end_src

* Syntax Checking (Flymake)

#+begin_src emacs-lisp
;; Flymake - built-in syntax checking (integrates with eglot)
(use-package flymake
  :straight nil
  :hook (prog-mode . flymake-mode)
  :bind (:map flymake-mode-map
              ("M-n" . flymake-goto-next-error)
              ("M-p" . flymake-goto-prev-error)
              ("C-c ! l" . flymake-show-buffer-diagnostics)
              ("C-c ! L" . flymake-show-project-diagnostics))
  :config
  (setq flymake-no-changes-timeout 0.5)  ; Run checkers 0.5s after last edit
  (setq flymake-start-on-save-buffer t)) ; Also re-check on save

;; Flymake-collection - additional checkers for flymake
(use-package flymake-collection
  :after flymake
  :config
  (flymake-collection-hook-setup))
#+end_src

* Miscellaneous Settings

#+begin_src emacs-lisp
;; Custom file location
(setq custom-file (expand-file-name "custom.el" user-emacs-directory))
(when (file-exists-p custom-file)
  (load custom-file))

;; Enable disabled commands
(put 'upcase-region 'disabled nil)
(put 'downcase-region 'disabled nil)
(put 'erase-buffer 'disabled nil)
(put 'set-goal-column 'disabled nil)

;; Ediff preferences
(setq ediff-window-setup-function 'ediff-setup-windows-plain) ; Use single frame, no separate control frame
(with-eval-after-load 'outline
  (add-hook 'ediff-prepare-buffer-hook #'org-show-all))

;; Eldoc and help settings (default 0.5s — raised to reduce noise)
(setq eldoc-idle-delay 3)
(setq help-at-pt-timer-delay 3)

;; Minibuffer can grow to 25% of frame height (default is ~40%)
(setq max-mini-window-height 0.25)

;; Wait 60s for LSP server to start (default 30s, needed for slow servers)
(setq eglot-connect-timeout 60)

;; Org-mode startup folding
(setq org-startup-folded 'show3levels)

;; Org-mode visual polish
(setq org-hide-emphasis-markers t)
(setq org-pretty-entities t)
(setq org-ellipsis " ▾")

;; Yasnippet directories - add custom snippets directory
(with-eval-after-load 'yasnippet
  (add-to-list 'yas-snippet-dirs "~/.emacs.d/snippets" t))

;; Sort words in region
(defun sort-words (reverse beg end)
  "Sort words in region alphabetically, in REVERSE if negative.
Prefixed with negative \\[universal-argument], sorts in reverse.

The variable `sort-fold-case' determines whether alphabetic case
affects the sort order.

See `sort-regexp-fields'."
  (interactive "*P\nr")
  (sort-regexp-fields reverse "\\w+" "\\&" beg end))


;; Window management - toggle maximize buffer
(defun toggle-maximize-buffer ()
  "Maximize buffer"
  (interactive)
  (if (= 1 (length (window-list)))
      (jump-to-register '_)
    (progn
      (window-configuration-to-register '_)
      (delete-other-windows))))
(global-set-key (kbd "C-c m b") 'toggle-maximize-buffer)

;; Clean theme switching - disable previous theme before loading new one
(defun disable-all-themes ()
  "Disable all active themes."
  (dolist (i custom-enabled-themes)
    (disable-theme i)))
(advice-add 'load-theme :before (lambda (&rest _) (disable-all-themes)))
#+end_src

* Utility Packages

#+begin_src emacs-lisp
;; Which-key - show available keybindings
(use-package which-key
  :config
  (which-key-mode))

;; Nerd Icons
(use-package nerd-icons
  :ensure t)

(use-package dirvish
  :ensure t
  :init
  (dirvish-override-dired-mode)
  :bind (:map dirvish-mode-map
         ("TAB" . dirvish-subtree-toggle))
  :custom
  (dired-listing-switches "-aBhl") ; -a all, -B ignore backups, -h human sizes, -l long format
  (dirvish-attributes '(nerd-icons subtree-state)))

;; NOTE: The package recommends using a marginalia-mode hook, but since
;; marginalia-mode is activated in :init (before nerd-icons-completion loads),
;; the hook has already fired. Direct call here is equivalent since
;; marginalia-mode is never toggled mid-session.
(use-package nerd-icons-completion
  :ensure t
  :after marginalia
  :config
  (nerd-icons-completion-mode)
  (nerd-icons-completion-marginalia-setup))

;; TODO: Review - GUI Emacs handles clipboard natively, these may be unnecessary.
;; Only needed if running Emacs in terminal mode (emacs -nw).
;; (defun copy-from-osx ()
;;   (shell-command-to-string "pbpaste"))
;; (defun paste-to-osx (text &optional push)
;;   (let ((process-connection-type nil))
;;     (let ((proc (start-process "pbcopy" "*Messages*" "pbcopy")))
;;       (process-send-string proc text)
;;       (process-send-eof proc))))
;; (setq interprogram-cut-function 'paste-to-osx)
;; (setq interprogram-paste-function 'copy-from-osx)

;; Mark ring navigation
(setq set-mark-command-repeat-pop t) ; After C-u C-SPC, repeat with just C-SPC to cycle mark ring

;; Load MacOS PATH
(use-package exec-path-from-shell
  :config
  (when (memq window-system '(mac ns x))
    (exec-path-from-shell-initialize)))

;; Avy - jump to visible text
(use-package avy
  :bind* ("C-;" . avy-goto-char-2))

;; Diff-hl - show git diff in fringe
(use-package diff-hl
  :config
  (global-diff-hl-mode t))

;; Ace-window - quickly switch windows
(use-package ace-window
  :bind (("M-o" . ace-window))
  :config
  (setq aw-keys '(?a ?s ?d ?f ?g ?h ?j ?k ?l)) ; Use home row keys for window labels
  (setq aw-scope 'frame))                      ; Only label windows in current frame

;; Moody - better mode-line
(use-package moody
  :config
  (setq x-underline-at-descent-line t) ; Draw underline at bottom of font glyph (fixes moody appearance)
  (moody-replace-mode-line-buffer-identification)
  (moody-replace-vc-mode))

;; Minions - compact minor modes display
(use-package minions
  :config
  (setq minions-mode-line-lighter "⚙"
        minions-mode-line-delimiters (cons "" ""))
  (minions-mode 1))

;; Rainbow-delimiters - colorize parentheses
(use-package rainbow-delimiters
  :hook (prog-mode . rainbow-delimiters-mode))
(message "Happy days")
#+end_src

* Terminal (vterm)

#+begin_src emacs-lisp
(use-package vterm
  :bind ("C-c t" . vterm)
  :config
  (setq vterm-max-scrollback 10000)
  (setq vterm-kill-buffer-on-exit t)
  ;; Workaround for vterm display corruption (input overlaps previous output).
  ;; Vterm's Emacs buffer can desync from libvterm's internal state.
  ;; A brief window resize sends SIGWINCH to the child process (e.g. Claude Code),
  ;; forcing a full TUI repaint that brings everything back in sync.
  (defun ando/vterm-refresh ()
    "Force vterm to re-sync by briefly resizing the window."
    (interactive)
    (when (eq major-mode 'vterm-mode)
      (enlarge-window-horizontally 1)
      (sit-for 0.1)
      (shrink-window-horizontally 1)))
  (define-key vterm-mode-map (kbd "C-c r") #'ando/vterm-refresh))
#+end_src

* AI Tools

** Claude Code IDE

#+begin_src emacs-lisp
(use-package claude-code-ide
  :straight (:host github :repo "manzaltu/claude-code-ide.el")
  :bind ("C-c C-'" . claude-code-ide-menu)
  :config
  (claude-code-ide-emacs-tools-setup)
  ;; Disable line numbers in vterm buffers
  (add-hook 'vterm-mode-hook (lambda () (display-line-numbers-mode -1))))
#+end_src

** GitHub Copilot

#+begin_src emacs-lisp
(use-package copilot
  :straight (:host github :repo "copilot-emacs/copilot.el" :branch "main")
  :hook (prog-mode . copilot-mode)
  :bind (:map copilot-completion-map
              ("<tab>" . ando/copilot-or-corfu-tab)
              ("TAB" . ando/copilot-or-corfu-tab))
  :config
  (defun ando/copilot-or-corfu-tab ()
    "Accept copilot completion, or fall through to corfu if its popup is active."
    (interactive)
    (if (and (bound-and-true-p corfu-mode)
             (frame-live-p (bound-and-true-p corfu--frame))
             (frame-visible-p corfu--frame))
        (corfu-next 1)
      (copilot-accept-completion))))
#+end_src
